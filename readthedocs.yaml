version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    - requirements: requirements.txt     # your package deps
    - requirements: docs/requirements.txt
    - method: pip
      path: .   # install your package
